#This class will be the bot's internal representation of the maze

class Graph:

	def __init__(self):
		self.dict={}

	def addNode(self,node):
		self.dict[len(dict):set()] #number each node distinctly, create a new set to represent adjacencies




class Node:

	def __init__(self):
		self.connections=[]

	def addConnection(self,node):
		self.connections.append(node)